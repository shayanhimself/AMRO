package com.shayan.amro.core.model

/**
 * The app's own genre vocabulary, which every source maps its own onto.
 *
 * These are identifiers, not copy: the UI should resolve each constant to a string resource.
 */
enum class Genre {
    ACTION,
    ADVENTURE,
    ANIMATION,
    COMEDY,
    CRIME,
    DOCUMENTARY,
    DRAMA,
    FAMILY,
    FANTASY,
    HISTORY,
    HORROR,
    MUSIC,
    MYSTERY,
    ROMANCE,
    SCIENCE_FICTION,
    TV_MOVIE,
    THRILLER,
    WAR,
    WESTERN,
}
